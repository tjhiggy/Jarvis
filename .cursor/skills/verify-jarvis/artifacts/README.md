# verify-jarvis artifacts

This directory is the named evidence path for `.cursor/skills/verify-jarvis`.

Cleanup **must not** delete this directory or the files under it.

| Path | What it holds |
| --- | --- |
| `seed-proof/` | First end-to-end proof that the generated skill works on a Cloud Agent VM |
| `<feature-id>/` | Later `drive-feature.sh` transcripts (`verify.txt`, `meta.txt`) |

Do not place `.env`, tokens, guild IDs, message content, or SQLite files here.
