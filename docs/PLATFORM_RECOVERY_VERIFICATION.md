# Platform Recovery Verification Matrix

This deterministic matrix maps the v1.6 recovery claims to disposable, synthetic Vitest evidence. It never reads production configuration, provider credentials, Discord state, or production SQLite data.

| ID                                    | Group        | Claim                                                                                                                          | Evidence                             | Recovery                                                                                                                         | Defect |
| ------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- | ------ |
| command-deck-local-fallback           | platform     | The localhost Command Deck remains available when the Sites surface is stale or offline.                                       | `tests/command-deck-cutover.test.ts` | Stop using the Sites URL and operate from http://127.0.0.1:8787 or Discord fallback commands.                                    | None   |
| command-deck-origin-allowlist         | platform     | Remote Command Deck reads from an unlisted origin are denied without leaking secrets.                                          | `tests/command-deck-cutover.test.ts` | Set COMMAND_DECK_API_ALLOWED_ORIGINS to the exact private Sites page origin and retry.                                           | None   |
| command-deck-token-rotation           | platform     | A rotated Command Deck read token is rejected until Jarvis restarts with the new secret.                                       | `tests/command-deck-cutover.test.ts` | Update the Sites read secret and Jarvis COMMAND_DECK_API_TOKEN together, restart Jarvis, then confirm a fresh snapshot succeeds. | None   |
| platform-configuration-validation     | platform     | Invalid required configuration is rejected without exposing supplied secrets.                                                  | `tests/config.test.ts`               | Correct the named configuration category and restart with approved values only.                                                  | None   |
| platform-feature-flags                | platform     | Feature flags preserve safe defaults and reject unsupported overrides.                                                         | `tests/feature-flags.test.ts`        | Inspect the durable feature state and restore the approved override before resuming work.                                        | None   |
| platform-global-pause                 | platform     | A persisted global pause blocks broadcast delivery before posting.                                                             | `tests/broadcast-policy.test.ts`     | Keep the global pause active until policy and destination checks are ready to resume.                                            | None   |
| platform-operational-audit-records    | platform     | Operational audit records accept bounded metadata and redact content.                                                          | `tests/moderation-audit.test.ts`     | Review only bounded audit metadata while investigating an operational event.                                                     | None   |
| platform-version-deployment-identity  | platform     | Version and deployment identity use trusted build metadata without exposing host details.                                      | `tests/runtime-question.test.ts`     | Confirm the reported version and build identity from trusted metadata before making a rollout decision.                          | None   |
| provider-github-published-state       | provider     | GitHub published availability follows the safe runtime health snapshot, not configuration presence alone.                      | `tests/provider-recovery.test.ts`    | Do not publish GitHub recovery until a new safe health snapshot succeeds.                                                        | None   |
| provider-ollama-published-state       | provider     | Ollama published availability follows the safe runtime health snapshot, not configuration presence alone.                      | `tests/provider-recovery.test.ts`    | Do not publish Ollama recovery until a new safe health snapshot succeeds.                                                        | None   |
| provider-openai-published-state       | provider     | OpenAI published availability follows the safe runtime health snapshot, not configuration presence alone.                      | `tests/provider-recovery.test.ts`    | Do not publish OpenAI recovery until a new safe health snapshot succeeds.                                                        | None   |
| provider-recovered-state              | provider     | Published provider health snapshots transition unavailable to ready after a successful probe.                                  | `tests/provider-recovery.test.ts`    | Do not mark the provider recovered until a new safe health snapshot succeeds.                                                    | None   |
| provider-rss-published-state          | provider     | RSS published availability follows configured-and-runtime-available health state.                                              | `tests/provider-recovery.test.ts`    | Do not publish RSS recovery until a new safe health snapshot succeeds.                                                           | None   |
| provider-sleeper-published-state      | provider     | Sleeper published availability follows the safe runtime health snapshot, not configuration presence alone.                     | `tests/provider-recovery.test.ts`    | Do not publish Sleeper recovery until a new safe health snapshot succeeds.                                                       | None   |
| provider-unavailable-state            | provider     | Published provider health snapshots report unavailable when runtime probes fail.                                               | `tests/provider-recovery.test.ts`    | Restore the provider endpoint, then confirm a new safe health snapshot reports ready.                                            | None   |
| provider-web-search-published-state   | provider     | Web-search published availability follows the safe runtime health snapshot, not configuration presence alone.                  | `tests/provider-recovery.test.ts`    | Do not publish web-search recovery until a new safe health snapshot succeeds.                                                    | None   |
| sanitization-command-deck             | sanitization | The Command Deck serves a secret-free local dashboard and JSON snapshot.                                                       | `tests/admin-console.test.ts`        | Use the local dashboard for state only and keep credentials outside its responses.                                               | None   |
| sanitization-operational-logs         | sanitization | Operational logs redact secret fields and omit error content.                                                                  | `tests/logger.test.ts`               | Preserve only sanitized categories and metadata while investigating the failure.                                                 | None   |
| sanitization-operational-metrics      | sanitization | Platform metrics store bounded content-free delivery aggregates.                                                               | `tests/platform-metrics.test.ts`     | Inspect aggregate metrics only; do not collect message or provider payload content.                                              | None   |
| scheduler-cadence-enforcement         | scheduler    | Broadcast policy blocks delivery until the configured minimum cadence has elapsed.                                             | `tests/broadcast-policy.test.ts`     | Wait for the configured cadence window instead of forcing a duplicate delivery.                                                  | None   |
| scheduler-claim-fencing               | scheduler    | Stale broadcast workers cannot complete delivery with an obsolete lease token.                                                 | `tests/broadcast-storage.test.ts`    | Allow the active lease owner to finish or expire before retrying delivery.                                                       | None   |
| scheduler-draining-shutdown           | scheduler    | Shutdown drains an in-flight RSS tick before storage closes.                                                                   | `tests/application.test.ts`          | Request shutdown once and wait for the active tick to drain before restart.                                                      | None   |
| scheduler-overlap                     | scheduler    | Overlapping reminder ticks share one active run.                                                                               | `tests/reminder-scheduler.test.ts`   | Wait for the active tick to finish before starting a replacement scheduler.                                                      | None   |
| scheduler-pause-race                  | scheduler    | A recap pause race releases its lease without posting.                                                                         | `tests/recap-scheduler.test.ts`      | Keep the feature paused and resume only after confirming the lease was released.                                                 | None   |
| scheduler-recurring-personal-reminder | scheduler    | A delivered personal recurring reminder advances the same row to the next bounded due time without inserting another reminder. | `tests/reminder-scheduler.test.ts`   | Leave the live row in place. Do not insert replacement one-shot rows after a fire.                                               | None   |
| scheduler-retry-release               | scheduler    | Transient reminder failures receive bounded retries and exhausted work fails safely.                                           | `tests/reminder-scheduler.test.ts`   | Let bounded retry scheduling run; investigate exhausted items before manual action.                                              | None   |
| scheduler-stale-lease-recovery        | scheduler    | Expired reminder claims recover while uncertain delivery is never automatically reclaimed.                                     | `tests/reminder-storage.test.ts`     | Recover only expired safe claims and investigate uncertain delivery before retrying.                                             | None   |
| scheduler-suppression-release         | scheduler    | Policy-suppressed event reminders release their claim so they remain retryable.                                                | `tests/broadcast-adopters.test.ts`   | Keep delivery suppressed until policy resumes; the released claim can then retry safely.                                         | None   |
| storage-backup-and-restore            | storage      | WAL-safe backup of the shared database restores conversation, engagement, poll, reminder, broadcast, and RSS sentinels.        | `tests/storage-recovery.test.ts`     | Stop Jarvis, checkpoint WAL, copy the database, restore to a new path, and verify sentinel rows plus integrity.                  | None   |
| storage-fresh-migration               | storage      | Fresh SQLite migration creates the required schema in disposable test state.                                                   | `tests/engagement-storage.test.ts`   | Run the focused storage test against its temporary database before rollout.                                                      | None   |
| storage-integrity-check               | storage      | SQLite integrity-check passes on a shared database after all schema owners migrate and write synthetic sentinels.              | `tests/storage-recovery.test.ts`     | Run PRAGMA integrity_check against a stopped disposable database before accepting a backup.                                      | None   |
| storage-legacy-migration              | storage      | Legacy active introductions migrate without discarding the recoverable newer record.                                           | `tests/engagement-storage.test.ts`   | Stop the runtime, back up the database, then reproduce the migration on a disposable copy.                                       | None   |
| storage-reopen-idempotence            | storage      | Shared-database reopen after all schema owners migrate is idempotent and remains integrity-clean.                              | `tests/storage-recovery.test.ts`     | Stop the runtime, reopen against a disposable copy of the shared database, and confirm every store health check still passes.    | None   |
| storage-restart-recovery              | storage      | A creating poll is reconciled after restart without Discord delivery.                                                          | `tests/poll-storage.test.ts`         | Restart against a disposable database and confirm only durable state reconciliation runs.                                        | None   |
| storage-rollback-classification       | storage      | A database with a newer user_version is refused and classified as requiring a pre-upgrade backup restore.                      | `tests/storage-recovery.test.ts`     | Do not open a newer schema with an older binary. Restore a pre-upgrade backup taken with a matching application version.         | None   |
| test-environment-runtime-evidence     | sanitization | The focused verifier writes a sanitized local receipt after executing the exact catalog evidence set.                          | `tests/recovery-receipt.test.ts`     | Run the focused verifier and inspect only its aggregate receipt and redaction result.                                            | None   |
